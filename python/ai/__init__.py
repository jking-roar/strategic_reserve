from game_engine import GameState


def get_move(game_state: GameState, player_color: str) -> tuple[int, int] | None:
    """AI integration contract for future stories."""
    _ = (game_state, player_color)
    return None

